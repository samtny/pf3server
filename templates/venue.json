{% block content %}
{
  "id": {{ venue.id }},
  "name": "{{ venue.name }}",
  "street": "{{ venue.street }}",
  "city": "{{ venue.city }}",
  "state": "{{ venue.state }}",
  "zipcode": "{{ venue.zipcode }}",
  "phone": "{{ venue.phone }}",
  "url": "{{ venue.url }}",
  "status": "{{ venue.status }}",
  "created": "{{ venue.created.format('c') }}"
}
{% endblock %}