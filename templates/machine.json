{% block content %}
{
{% if machine.id %}"id": {{ machine.id }},{% endif %}
"condition": {{ machine.condition }},
"price": "{{ machine.price }}",
"name": "{{ machine.game.name }}",
"ipdb": {{ machine.game.ipdb }},
"created": "{{ machine.created.format('c') }}"
}
{% endblock %}