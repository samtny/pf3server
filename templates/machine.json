{% block content %}
{
{% if machine.id %}"id": {{ machine.id }},{% endif %}
{% if machine.condition %}"condition": {{ machine.condition }},{% endif %}
{% if machine.price %}"price": {{ machine.price }},{% endif %}
"created": "{{ machine.created.format('c') }}"
}
{% endblock %}